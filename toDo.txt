1. CreteGame should generate a room code after submit
    User should be able to go into waiting room by entering room code

In joinGame, after entering room code, username input should appear

In createGame, if creator participates in the game, username input should appear and be required for submit
    if not, just proceed

Next: users should only be able to join an existing room with code
        Client sends enter request to server
        Server checks if room already exists. Checks also # of players
        Joining a game puts you in a server room
        Add empty space in "How many players are in the game" form to force a choice


        create room sends create message with room code to server
        join room sends join message with room code to server